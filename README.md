# Steps to Run the Project

# Step 1: Load Users.sql to create the user and grant required permissions.

# Step 2: Load DDL.sql to create tables and constraints.

# Step 3: Load DML(1).sql and DML(2).sql respectively to insert the data into tables.

